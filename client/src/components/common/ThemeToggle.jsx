import React from 'react';
import { ThemeToggleButton2 } from '../ui/skiper-ui/skiper4';

export const ThemeToggle = ({ className = 'size-9 p-1.5' }) => {
  return <ThemeToggleButton2 className={className} />;
};

export default ThemeToggle;
